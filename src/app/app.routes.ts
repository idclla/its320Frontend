import { Routes } from '@angular/router';
import { DisplayQuotesComponent } from './displayqoutes/displayqoutes.component';

export const routes: Routes = [
  { path: 'quotes', component: DisplayQuotesComponent }
];
